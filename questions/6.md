6. Let’s next look at main-signal.c. This code uses a variable (done)
to signal that the child is done and that the parent can now continue.
Why is this code inefficient? (what does the parent end up spend-
ing its time doing, particularly if the child thread takes a long time
to complete?)


* there is no pthread join that waits for the thread to finish before exiting the code, instead there is a while statement that loops through nothing waiting CPU usage while it waits for done to get updated
* if the parent thread beats the child thread then while its waiting, its doing nothing 